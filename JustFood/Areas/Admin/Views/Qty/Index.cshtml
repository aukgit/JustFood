@model IEnumerable<JustFood.Models.QuantityType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QtyType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QtyType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuantityTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuantityTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuantityTypeID })
        </td>
    </tr>
}

</table>
